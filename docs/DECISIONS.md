# Decision log

One line per irreversible/architectural decision, dated. Newest first.

- 2026-07-12 — Runtime robot switch = persist + reload (not in-place re-init): assembly/wiring/scene/sim resolve their RobotDef once at init (baked slot-ghost meshes, the tray DOM, `WiringManager.required`, the sim body), so a full teardown/rebuild would be fragile in a zero-build vanilla app. `switchRobot(id)` (js/robots/index.js) persists to `localStorage` (`sbl-active-robot`) and reloads; `bootActiveRobot()` reads it into state before `createScene`. `save.js` gates restore on matching `robotId`. Per-robot save slots deferred (single localStorage key today) — safe until a second robot is buildable.

- 2026-07-12 — Rover scaffolded (`js/robots/rover.js`, `available:false`): extracted from the inline index.js stub into its own def with full metadata + intended wiring as data (`plannedRequired`, skid-steer, 2× L298N). Empty parts/slots/required until built. Header enumerates the 5 steps to make it buildable (4-wheel parts/slots, rover REQUIRED, `RoverSim` body + sim-registry case, a rover lesson, per-robot save slots). No new physics — sim-registry still falls back to the balance body for unknown/rover keys.

- 2026-07-12 — Roadmap M4 (RobotDef seam activated): threaded `activeRobot()` through the app so the def — not module globals — is the live source of parts/slots/required/sketch. `WiringManager` now takes `required` as a constructor arg (default `activeRobot().required`); `findRequired`/`suggestFor` became instance methods over `this.required` (module `suggestFor`/`REQUIRED` exports kept for curriculum back-compat). `assembly.js`/`hud.js`/`guide.js`/`scene.js`/`save.js` read from `activeRobot()`. Sim body now built via `js/robots/sim-registry.js` `createSimBody(simKey, scene)` keyed by `simKey` instead of `new BalanceSim` in main. Pure refactor, zero behavior change — no sim tuning touched; all 10 functional Playwright tests green + manual drive-test. One buildable robot still, so consumers resolve the def once (a runtime robot switch will need re-init when the rover lands).

- 2026-07-12 — Roadmap M3 P0 (RobotDef seam): added `js/robots/` (RobotDef registry; self-balancer bundles the parts/slots/required/sketch globals + metadata; rover & line-follower declared `available:false`), `state.activeRobotId`, a robot picker popover off the top-bar chip, and bumped the save schema to v2 (`robotId`, v1→v2 migration). Data-level only — assembly/wiring still import globals directly, zero behavior change; sim.js body/controller extraction (keyed by `simKey`) deferred to M4 when the rover needs it.

- 2026-07-12 — Roadmap M1 (Lab-Instrument identity): committed amber-phosphor (#ffb000) as the signature accent replacing the generic blue #56a8ff (green reserved for connected/success, red for fault); added Space Grotesk display font (`--display`); tokenized button gradients + `--accent-soft`; added a top-bar shell (`js/app/topbar.js`, brand + robot chip + theme toggle) and reworked the onboarding modal into a Build→Program→Play title screen. See memory `visual-identity-decision`.

- 2026-07-11 — Roadmap M0 (foundation hygiene): resynced `CLAUDE.md` to the real module set; added `tests/docs.spec.js` doc-drift guard (every `js/` module must be named in CLAUDE.md — docs are now part of DoD); added dev-only perf HUD `js/app/perf.js` (`?perf`/Alt+P). Follows the 2026-07-11 market/UX/architecture deep dive (5 Notion briefs).

- 2026-07-10 — `_diag.mjs` kept (not deleted): it is the headless Playwright verification harness; Phase 1B formalizes it into `tests/`.
- 2026-07-10 — Master roadmap adopted (`docs/MASTER_PLAN.md`): vanilla web + design tokens (no React), Supabase backend, Capacitor for iOS, RevenueCat for unified entitlements.
- 2026-07-09 — Unity port abandoned; web app is the single active codebase.
